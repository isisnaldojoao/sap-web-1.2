import { FormLogin } from "../../components/FormLogin";
import { LoginContainer } from "./styles";

export const Login = () => {
  return (
    <LoginContainer>
      <FormLogin />
    </LoginContainer>
  );
};
